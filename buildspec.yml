version: 0.2

phases:
  build:
    commands:
      - aws deploy push --application-name aws-cicd-app --s3-location s3://nichika-final-bucket/artifact.zip --source src
artifacts:
  files:
    - '**/*'
  base-directory: src
